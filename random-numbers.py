
# random gera um número aleatório entre 0 e 1 (float number por padrão)
num = random.random()
print(num)

# random com um int e range definidos gera um número inteiro entre os números definidos
numInteiro = random.randint(1,10)
print(numInteiro)